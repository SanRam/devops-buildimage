---
- hosts: default
  tasks:
  - name: Install required packages for libvirt/Qemu.
    dnf:
      name:
        - epel-release
        - qemu-kvm
        - libvirt
        - libvirt-client
        - virt-manager
        - virt-install
        - virt-top
        - libguestfs-tools
        - virt-viewer
      state: latest
